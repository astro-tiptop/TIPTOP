version: 2

build:
  commands:
    - pwd
    - ls -alr

sphinx:
  builder: html
  configuration: ./docs/source/conf.py
  fail_on_warning: true

python:
  install:
    - method: pip
      path: .
      extra_requirements:
        - pyproject.toml


